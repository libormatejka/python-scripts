services:
  strava:
    build: .
    ports:
      - "8765:8765"
    env_file: ../.env
    volumes:
      - ../.strava_token.json:/app/.strava_token.json
      - ../google_credentials.json:/app/google_credentials.json
